# tools/portability-allowlist.txt
# CHECK 25 (ADR-0084 D1) portability-scrub allowlist.
#
# Format: <path>|<literal substring of the exempted line>|<reason>
# - path-anchored: an entry exempts only the named file, never repo-wide.
# - substring-anchored, never line-number-anchored: entries track the text,
#   so an unrelated insertion above the line cannot silently un-exempt or
#   mis-exempt it.
# - never a whole-file exemption: only the matched line is skipped.
# - a genuine new leak is ROTATED/removed, never allowlisted — this file is
#   for hazard-DOCUMENTING prose only (comments illustrating the hazard
#   CHECK 25 exists to catch), never for a real machine-local path.
#
# Seeded at landing (ADR-0084 D1) with the union of every Class A/B hit this
# ADR's measurement found outside qa-proof/**, decisions/**, and this file
# itself, at commit c06df1758c118a156d5da8367532768369847215.

bootstrap.sh|C:/Program Files/Git/repos/...|MSYS path-conversion illustration comment (Class B arm 1)
tests/test_promote_ack_880.py|C:\foo\bar|MSYS fallback-conversion illustration comment (Class B arm 1+2)
tests/test_promote_gate_parse_1036.py|C:\foo\bar|MSYS fallback-conversion illustration comment (Class B arm 1+2)
tests/test_promote_sentinel_order_1038.py|C:\foo\bar|MSYS fallback-conversion illustration comment (Class B arm 1+2)
tests/test_session_start_hookspath_1282.py|/c/Users/name|bash tilde-expansion hazard comment, placeholder account name (Class A+B arm 2)
tests/test_session_start_hookspath_1282.py|F:\project_claude\.githooks|worked example of an absolute hooksPath value (Class B arm 1)
tools/deploy-handshake.sh|F:\project_claude\.githooks|worked example of an absolute hooksPath value, drive-letter form (Class B arm 1)
tools/deploy-handshake.sh|/f/project_claude/.githooks|same worked example, MSYS mount form (Class B arm 2)
